🤖 Youtube Download Bot — Универсальный загрузчик

Telegram-бот для скачивания видео и аудио из:

🎵 TikTok

📸 Instagram (Reels)

▶️ YouTube

📹 VK

🔥 Ключевые особенности

✅ Оптимизирован для iPhone (исправляет проблему «застывшего кадра» в Instagram)

📦 Автоматически сжимает видео под лимиты Telegram (50 МБ)

⚡ Быстрая отдача без перекодировки, когда это возможно

🔄 Поддержка автозапуска и работы 24/7 через systemd

🚀 Установка на сервер (Ubuntu / Debian)
1️⃣ Подготовка системы

Обновите систему и установите необходимые пакеты
(FFmpeg обязателен):

sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip python3-venv ffmpeg git -y

2️⃣ Клонирование репозитория и установка зависимостей
git clone https://github.com/ВАШ_АККАУНТ/super_downloader_bot.git
cd super_downloader_bot


Создание виртуального окружения:

python3 -m venv venv
source venv/bin/activate


Установка зависимостей:

pip install -r requirements.txt

3️⃣ Настройка конфигурации

Отредактируйте файл:

src/config.py


или создайте .env файл, указав:

BOT_TOKEN=ВАШ_TELEGRAM_BOT_TOKEN
ADMIN_ID=123456789
CHANNEL_ID=@your_channel


CHANNEL_ID используется для обязательной подписки (опционально)

⚙️ Постоянная работа (systemd)

Чтобы бот работал 24/7 и автоматически запускался после перезагрузки сервера:

1️⃣ Создайте файл службы
sudo nano /etc/systemd/system/download_bot.service

2️⃣ Вставьте конфигурацию
[Unit]
Description=Telegram Download Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/super_downloader_bot
ExecStart=/root/super_downloader_bot/venv/bin/python run.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target

3️⃣ Активируйте и запустите службу
sudo systemctl daemon-reload
sudo systemctl enable download_bot
sudo systemctl start download_bot

🔄 Обновление бота

При изменениях в репозитории используйте следующий алгоритм:

1️⃣ Загрузите обновления
cd /root/super_downloader_bot
git pull origin main

2️⃣ Обновите зависимости (если нужно)
./venv/bin/pip install -r requirements.txt

3️⃣ Перезапустите бота
sudo systemctl restart download_bot

4️⃣ Проверьте статус
sudo systemctl status download_bot

🛠 Управление и логи

Просмотр логов в реальном времени:

journalctl -u download_bot -f


Остановка бота:

sudo systemctl stop download_bot


Перезапуск:

sudo systemctl restart download_bot

💎 Особенности реализации
📱 iPhone Fix (Instagram)

Для Instagram применяется глубокая перекодировка:

-pix_fmt yuv420p

-r 30

-vsync cfr

✔️ Полностью решает проблему зависших кадров на iOS.

⚡ Fast Transcoding

Для YouTube и TikTok используется режим:

-c copy


Если видео весит менее 45 МБ, файл отдается мгновенно без перекодировки.

📦 Auto-Compression

Для длинных видео бот:

автоматически рассчитывает битрейт

гарантирует итоговый размер ≤ 50 МБ

📢 Рекламный блок

В описание каждого видео автоматически добавляется:

🚀 Скачано через: @youtodownloadbot

✅ Готово

Бот полностью готов к работе и масштабированию.
При необходимости легко адаптируется под другие источники и лимиты.
